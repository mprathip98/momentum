services:
  - type: web
    name: reflex-app
    env: python
    plan: free
    buildCommand: reflex init && reflex db init && reflex export
    startCommand: reflex run --env prod
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: reflex-db
          property: connectionString
