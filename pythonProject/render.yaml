services:
  - type: web
    name: my-reflex-app
    env: python
    buildCommand: pip install -r requirements.txt && reflex build
    startCommand: reflex start
    autoDeploy: true
    envVars:
      - fromDatabase:
          name: reflex-db